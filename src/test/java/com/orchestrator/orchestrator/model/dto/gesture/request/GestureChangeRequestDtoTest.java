package com.orchestrator.orchestrator.model.dto.gesture.request;

import com.orchestrator.orchestrator.model.dto.DtoTest;

public class GestureChangeRequestDtoTest extends DtoTest<GestureChangeRequestDto> {
    @Override
    protected GestureChangeRequestDto getInstance() {
        return new GestureChangeRequestDto();
    }
}
