package com.orchestrator.orchestrator.model.dto.gesture.request;

import com.orchestrator.orchestrator.model.dto.DtoTest;

public class GestureUpdateRequestDtoTest extends DtoTest<GestureUpdateRequestDto> {
    @Override
    protected GestureUpdateRequestDto getInstance() {
        return new GestureUpdateRequestDto();
    }
}