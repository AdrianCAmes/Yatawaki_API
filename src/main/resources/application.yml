# Spring config
spring:
  application:
    name: customer-info-service
  datasource:
    url: jdbc:sqlserver://development-yatawaki.database.windows.net;databaseName=DBOrchestrator
    driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver
    username: yadmin
    password: Contra123
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.SQLServer2012Dialect
    hibernate:
      ddl-auto: update